pipeline {
    agent any

    stages {
        stage('Vérification') {
            steps {
                echo '✅ Jenkins fonctionne correctement avec ce Jenkinsfile !'
            }
        }
    }
}
